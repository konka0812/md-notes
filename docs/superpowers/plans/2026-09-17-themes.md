# 新增两个主题（打字机 / 森系）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有「纸张」「手绘」之外新增「复古打字机」与「森系自然」两个主题（各含浅色/深色），并把主题选择菜单改成「风格 → 明暗」两级。

**Architecture:** 沿用现有 CSS 变量 + `[data-style="..."]` 结构覆盖的机制，只改 `styles.css` 与 `app.js`。`app.js` 里 `showThemePicker()` 由一层平铺列表改为两级 sheet（复用现有 `showSheet`），`applyTheme()` 增加「按风格+明暗查表」更新 `theme-color`。

**Tech Stack:** 原生 HTML/CSS/JS（无构建步骤）、Playwright（Python）测试、`python3 -m http.server` 本地预览。

**Spec:** `docs/superpowers/specs/2026-09-17-themes-and-file-sync-design.md` 第一部分

---

## 文件结构

| 文件 | 职责 | 本次改动 |
| --- | --- | --- |
| `styles.css` | 设计系统：CSS 变量 + 主题结构覆盖 | 追加 `typewriter` / `forest` 两段主题样式 |
| `app.js` | 应用逻辑 | 新增 `THEME_STYLES` 常量、`themeColorFor()`、重写 `showThemePicker()`、替换 `applyTheme()` 里写死的 theme-color |
| `scripts/test-themes.py` | 主题功能测试 | 新建 |
| `scripts/test-themes-shot.py` | 主题截图脚本 | 新建 |
| `sw.js` / `version.json` | 缓存与版本 | 版本号 v12 → v13 |

## 已有代码现状（工程师必读）

- 主题由 `document.documentElement` 上的两个属性决定：`data-theme`（`light`/`dark`）、`data-style`（`''`=纸张 / `sketch`=手绘）。
- `app.js:1133` 的 `applyTheme(t, style)` 负责设置属性、写 `localStorage`、切换月亮/太阳图标、更新 `<meta name="theme-color">`。
- `app.js:1153` 的 `showThemePicker()` 目前平铺 4 项，调用 `showSheet(title, items)`。
- `showSheet(title, items)`（`app.js:857`）签名：`items` 是 `{ icon?, label, action, danger? }[]`，点击后自动 `hideSheet()` 再执行 `action`。**可以在 `action` 里再次调用 `showSheet` 实现二级菜单。**
- `styles.css:724` 起是 `[data-style="sketch"]` 主题，新增主题请在文件末尾（`打印 / 导出 PDF` 段落之前，即 `styles.css:817` 之前）插入。
- 版本号三处必须同步：`app.js:85` 的 `APP_VERSION`、`sw.js:7` 的 `CACHE`、`version.json`。

## 测试环境准备（每个任务开始前执行一次）

```bash
cd /Users/konka/Desktop/work/md-notes
python3 -m http.server 8123 > /tmp/zhimo-http.log 2>&1 &
sleep 1
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8123/index.html
```

预期输出：`200`

---

### Task 1: 主题常量与 theme-color 查表

**Files:**
- Modify: `app.js:85`（新增常量）、`app.js:1133-1141`（`applyTheme`）、`app.js:1153-1171`（`showThemePicker`）

- [ ] **Step 1: 写失败测试**

新建 `scripts/test-themes.py`：

```python
#!/usr/bin/env python3
"""主题测试：四种风格 × 明暗切换、两级菜单、theme-color"""
import sys
from playwright.sync_api import sync_playwright

BASE = "http://localhost:8123"
results = []
errors = []

def check(name, cond, extra=""):
    results.append((name, bool(cond)))
    print(("  PASS  " if cond else "  FAIL  ") + name + (("  [" + str(extra) + "]") if extra else ""))

with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    page = browser.new_context(viewport={"width": 390, "height": 844}, is_mobile=True, has_touch=True).new_page()
    page.on("pageerror", lambda e: errors.append(str(e)))
    page.goto(BASE, wait_until="networkidle")

    # 先建一篇笔记，供列表卡片样式断言使用
    page.click("#btn-new")
    page.wait_for_selector("#editor-view.active")
    page.fill("#note-title", "主题测试")
    page.fill("#editor", "# 标题\n\n正文")
    page.click("#btn-back")
    page.wait_for_selector("#list-view.active")

    def state():
        return page.evaluate("""() => ({
            theme: document.documentElement.getAttribute('data-theme'),
            style: document.documentElement.getAttribute('data-style'),
            color: document.querySelector('meta[name="theme-color"]').getAttribute('content'),
            lsTheme: localStorage.getItem('theme'),
            lsStyle: localStorage.getItem('style')
        })""")

    def pick(style_label, theme_label):
        page.click("#btn-more")
        page.locator("#sheet .sheet-item", has_text="主题").click()
        page.wait_for_timeout(200)
        page.locator("#sheet .sheet-item", has_text=style_label).click()
        page.wait_for_timeout(200)
        page.locator("#sheet .sheet-item", has_text=theme_label).click()
        page.wait_for_timeout(300)

    # 默认：纸张浅色
    s = state()
    check("默认 data-style 为空(纸张)", s["style"] == "", s["style"])

    # 打字机 · 深色
    pick("复古打字机", "深色")
    s = state()
    check("打字机 深色 data-style", s["style"] == "typewriter", s["style"])
    check("打字机 深色 data-theme", s["theme"] == "dark", s["theme"])
    check("打字机 深色 theme-color", s["color"] == "#16140F", s["color"])
    check("打字机 深色 已持久化", s["lsStyle"] == "typewriter" and s["lsTheme"] == "dark")

    # 森系 · 浅色
    pick("森系自然", "浅色")
    s = state()
    check("森系 浅色 data-style", s["style"] == "forest", s["style"])
    check("森系 浅色 data-theme", s["theme"] == "light", s["theme"])
    check("森系 浅色 theme-color", s["color"] == "#FAFCF6", s["color"])

    # 顶部月亮按钮：只在当前风格内切明暗
    page.click("#btn-theme")
    page.wait_for_timeout(200)
    s = state()
    check("月亮按钮切到森系深色", s["theme"] == "dark" and s["style"] == "forest", str(s))

    # 刷新后保持
    page.reload(wait_until="networkidle")
    s = state()
    check("刷新后保持森系深色", s["theme"] == "dark" and s["style"] == "forest", str(s))

    # 回到纸张深色（供其余测试的默认状态用）
    pick("纸张", "深色")
    s = state()
    check("纸张 深色 theme-color", s["style"] == "" and s["theme"] == "dark" and s["color"] == "#1E1C17", str(s))

    browser.close()

print(f"\n{sum(1 for _, c in results if c)}/{len(results)} 通过")
if errors:
    print("JS 错误：", errors[:5])
sys.exit(0 if all(c for _, c in results) and not errors else 1)
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python3 scripts/test-themes.py
```

预期：`复古打字机` / `森系自然` 两项找不到而超时失败（`TimeoutError`），或 `data-style` 断言 FAIL。

- [ ] **Step 3: 实现常量与查表函数**

在 `app.js:85` 把版本号与常量一起改掉：

```javascript
const APP_VERSION = 'v13';

/* ---------------- 主题风格 ---------------- */
const THEME_STYLES = [
  { id: '',           label: '纸张',       color: { light: '#FDFBF7', dark: '#1E1C17' } },
  { id: 'sketch',     label: '手绘',       color: { light: '#FDFBF7', dark: '#1E1C17' } },
  { id: 'typewriter', label: '复古打字机', color: { light: '#FCF9F0', dark: '#16140F' } },
  { id: 'forest',     label: '森系自然',   color: { light: '#FAFCF6', dark: '#111A12' } }
];
function themeColorFor(theme, style) {
  const s = THEME_STYLES.find((x) => x.id === (style || ''));
  const base = s || THEME_STYLES[0];
  return base.color[theme === 'dark' ? 'dark' : 'light'];
}
```

- [ ] **Step 4: 改写 applyTheme 使用查表**

把 `app.js:1133-1141` 的 `applyTheme` 整体替换为：

```javascript
function applyTheme(t, style) {
  document.documentElement.setAttribute('data-theme', t);
  document.documentElement.setAttribute('data-style', style || '');
  localStorage.setItem('theme', t);
  localStorage.setItem('style', style || '');
  btnTheme.innerHTML = icon(t === 'dark' ? 'sun' : 'moon', 22);
  const mc = document.querySelector('meta[name="theme-color"]');
  if (mc) mc.setAttribute('content', themeColorFor(t, style));
}
```

- [ ] **Step 5: 把主题菜单改成两级**

把 `app.js:1153-1171` 的 `showThemePicker` 整体替换为：

```javascript
function showThemePicker() {
  const curStyle = document.documentElement.getAttribute('data-style') || '';
  const items = THEME_STYLES.map((s) => ({
    icon: s.id === curStyle ? 'check' : '',
    label: s.label,
    action: () => showThemeDarkPicker(s)
  }));
  showSheet('选择风格', items);
}

function showThemeDarkPicker(style) {
  const curTheme = document.documentElement.getAttribute('data-theme') || 'light';
  showSheet(style.label + ' · 明暗', [
    { icon: curTheme === 'light' ? 'check' : '', label: '浅色', action: () => { applyTheme('light', style.id); toast('已切换：' + style.label + ' · 浅色'); } },
    { icon: curTheme === 'dark' ? 'check' : '', label: '深色', action: () => { applyTheme('dark', style.id); toast('已切换：' + style.label + ' · 深色'); } }
  ]);
}
```

- [ ] **Step 6: 运行测试确认通过**

```bash
python3 scripts/test-themes.py
```

预期：全部 PASS，退出码 0。此时主题属性与 theme-color 已正确，但视觉尚未实现（下一步）。

- [ ] **Step 7: 提交**

```bash
git add app.js scripts/test-themes.py
git commit -m "主题：新增风格常量与两级主题选择菜单"
```

---

### Task 2: 复古打字机主题样式

**Files:**
- Modify: `styles.css`（在 `styles.css:817` 的「打印 / 导出 PDF」注释块之前插入）

- [ ] **Step 1: 写失败测试**

在 `scripts/test-themes.py` 的 `browser.close()` 之前插入以下代码（缩进与同级语句一致，仍在 `with sync_playwright()` 块内）：

```python
    # ---- 打字机主题的视觉特征 ----
    pick("复古打字机", "浅色")
    tw = page.evaluate("""() => {
        const cs = getComputedStyle(document.body);
        const item = getComputedStyle(document.querySelector('.note-item') || document.body);
        return {
            fontUI: cs.fontFamily,
            bg: cs.backgroundColor,
            accent: getComputedStyle(document.documentElement).getPropertyValue('--accent').trim(),
            radius: item.borderRadius,
            borderStyle: item.borderStyle
        };
    }""")
    check("打字机 UI 字体含 Courier", "Courier" in tw["fontUI"], tw["fontUI"])
    check("打字机 强调色为印章红", tw["accent"].upper() == "#A8321E", tw["accent"])
    check("打字机 双线边框", "double" in tw["borderStyle"], tw["borderStyle"])
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python3 scripts/test-themes.py
```

预期：`打字机 UI 字体含 Courier`、`打字机 强调色为印章红`、`打字机 双线边框` 三项 FAIL。

- [ ] **Step 3: 写入主题样式**

在 `styles.css` 中「`/* =====...  打印 / 导出 PDF`」注释块**之前**插入：

```css
/* =========================================================================
   复古打字机主题（data-style="typewriter"）—— 泛黄打字纸 × 双线边框 × 印章红
   ========================================================================= */
[data-style="typewriter"] {
  --bg:        #F6F1E2;
  --paper:     #FCF9F0;
  --ink:       #2A241C;
  --ink-soft:  #554A3B;
  --ink-faint: #8A7C67;
  --border:    #3B3228;
  --accent:    #A8321E;
  --accent-tint:#F0DED2;
  --danger:    #8F2416;
  --danger-tint:#F3DAD4;
  --shadow-sm: 2px 2px 0 rgba(59, 50, 40, 0.18);
  --radius:    3px;
  --font-ui: "Courier New", Courier, "American Typewriter", "Songti SC", "STSong", "SimSun", monospace;
  --font-content: "Courier New", Courier, "Songti SC", "STSong", "SimSun", serif;
  --font-mono: "Courier New", Courier, Menlo, monospace;
}
[data-style="typewriter"][data-theme="dark"] {
  --bg:        #16140F;
  --paper:     #1E1B14;
  --ink:       #E8E0C8;
  --ink-soft:  #B9AF96;
  --ink-faint: #7C7259;
  --border:    #6B6045;
  --accent:    #D9705C;
  --accent-tint:#33291F;
  --danger:    #E08A80;
  --danger-tint:#3A2620;
  --shadow-sm: 2px 2px 0 rgba(0, 0, 0, 0.5);
}

/* 打字纸横格纹 */
[data-style="typewriter"] body {
  background-image: repeating-linear-gradient(
    180deg,
    rgba(59, 50, 40, 0.045) 0px,
    rgba(59, 50, 40, 0.045) 1px,
    transparent 1px,
    transparent 28px
  );
}
[data-style="typewriter"][data-theme="dark"] body {
  background-image: repeating-linear-gradient(
    180deg,
    rgba(232, 224, 200, 0.035) 0px,
    rgba(232, 224, 200, 0.035) 1px,
    transparent 1px,
    transparent 28px
  );
}

/* 双线边框：卡片 / 面板 / 对话框 / 按钮 / 搜索框 */
[data-style="typewriter"] .note-item,
[data-style="typewriter"] .search-wrap,
[data-style="typewriter"] .sheet,
[data-style="typewriter"] .dialog,
[data-style="typewriter"] .btn,
[data-style="typewriter"] .segmented,
[data-style="typewriter"] .markdown-body pre {
  border: 3px double var(--border);
}
[data-style="typewriter"] .note-item,
[data-style="typewriter"] .search-wrap,
[data-style="typewriter"] .dialog { border-radius: 3px; }
[data-style="typewriter"] .sheet { border-radius: 6px 6px 0 0; }
[data-style="typewriter"] .btn,
[data-style="typewriter"] .segmented { border-radius: 3px; }
[data-style="typewriter"] .markdown-body pre { border-radius: 3px; }

/* 小控件：细双线，避免过重 */
[data-style="typewriter"] .tool-btn,
[data-style="typewriter"] .quickbar-btn {
  border: 1.5px double var(--border);
  border-radius: 3px;
}
[data-style="typewriter"] .markdown-body code {
  border: 1px solid var(--border);
  border-radius: 2px;
}

/* FAB：双圈圆钮 */
[data-style="typewriter"] .fab {
  border-radius: 50%;
  box-shadow: 0 0 0 3px var(--paper), 0 0 0 5px var(--border), 2px 3px 0 5px rgba(59, 50, 40, 0.18);
}
[data-style="typewriter"] .icon-btn { border-radius: 50%; }

/* 标题：加字距 */
[data-style="typewriter"] .app-name,
[data-style="typewriter"] .note-item-title,
[data-style="typewriter"] .markdown-body h1,
[data-style="typewriter"] .markdown-body h2,
[data-style="typewriter"] .markdown-body h3 {
  letter-spacing: 0.12em;
  text-transform: none;
}

/* 标签：邮票 / 印章方块 */
[data-style="typewriter"] .tag-chip,
[data-style="typewriter"] .filter-chip {
  border-radius: 0;
  border: 1.5px solid currentColor;
  letter-spacing: 0.1em;
  padding: 1px 8px;
  text-transform: uppercase;
}

/* 分割线：打字机双线 */
[data-style="typewriter"] .markdown-body hr {
  border: none;
  border-top: 3px double var(--border);
}
[data-style="typewriter"] .tool-sep { background: var(--border); width: 2px; }

/* 引用：机械缩进感 */
[data-style="typewriter"] .markdown-body blockquote {
  border-left: 3px double var(--accent);
  border-radius: 0;
  background: transparent;
  font-style: italic;
}
[data-style="typewriter"] .markdown-body h1 { border-bottom: 3px double var(--border); }
[data-style="typewriter"] .appbar { border-bottom: 3px double var(--border); }
[data-style="typewriter"] .editor-footer,
[data-style="typewriter"] .toolbar,
[data-style="typewriter"] .quickbar { border-color: var(--border); }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python3 scripts/test-themes.py
```

预期：全部 PASS。

- [ ] **Step 5: 截图人工确认**

新建 `scripts/test-themes-shot.py`：

```python
#!/usr/bin/env python3
"""主题截图：四种风格 × 浅色/深色"""
import os
from playwright.sync_api import sync_playwright

BASE = "http://localhost:8123"
OUT = os.path.join(os.path.dirname(__file__), "shots")
os.makedirs(OUT, exist_ok=True)

STYLES = [("", "paper"), ("sketch", "sketch"), ("typewriter", "typewriter"), ("forest", "forest")]

with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    page = browser.new_context(viewport={"width": 390, "height": 844}, is_mobile=True, has_touch=True).new_page()
    page.goto(BASE, wait_until="networkidle")
    page.click("#btn-new")
    page.wait_for_selector("#editor-view.active")
    page.fill("#note-title", "主题预览")
    page.fill("#editor", "# 一级标题\n\n这是一段**正文**，用来观察字体与行距。\n\n- 列表项一\n- 列表项二\n\n> 引用文字\n\n`code` 与 [链接](https://example.com)\n")
    page.click("#btn-back")
    page.wait_for_selector("#list-view.active")

    for style_id, slug in STYLES:
        for theme in ("light", "dark"):
            page.evaluate(
                """([t, s]) => { document.documentElement.setAttribute('data-theme', t);
                                 document.documentElement.setAttribute('data-style', s); }""",
                [theme, style_id],
            )
            page.wait_for_timeout(250)
            page.screenshot(path=os.path.join(OUT, f"theme-{slug}-{theme}-list.png"))
            page.locator(".note-item").first.click()
            page.wait_for_selector("#editor-view.active")
            page.wait_for_timeout(250)
            page.screenshot(path=os.path.join(OUT, f"theme-{slug}-{theme}-editor.png"))
            page.click("#btn-back")
            page.wait_for_selector("#list-view.active")
    browser.close()

print("截图完成 ->", OUT)
```

运行：

```bash
python3 scripts/test-themes-shot.py && open scripts/shots
```

逐张检查 8 张 `theme-*.png`：字号、对比度、边框是否可读；深色模式下文字不得出现「黑底黑字」。

- [ ] **Step 6: 提交**

```bash
git add styles.css scripts/test-themes.py scripts/test-themes-shot.py
git commit -m "主题：复古打字机样式（打字纸、双线边框、印章红）"
```

---

### Task 3: 森系自然主题样式

**Files:**
- Modify: `styles.css`（紧接 Task 2 插入段之后）

- [ ] **Step 1: 写失败测试**

在 `scripts/test-themes.py` 的 `browser.close()` 之前插入：

```python
    # ---- 森系主题的视觉特征 ----
    pick("森系自然", "浅色")
    fo = page.evaluate("""() => {
        const item = getComputedStyle(document.querySelector('.note-item') || document.body);
        return {
            accent: getComputedStyle(document.documentElement).getPropertyValue('--accent').trim(),
            radius: item.borderRadius,
            shadow: item.boxShadow
        };
    }""")
    check("森系 强调色为苔绿", fo["accent"].upper() == "#4C7A3F", fo["accent"])
    check("森系 大圆角(>=16px)", float(fo["radius"].replace("px", "").split()[0]) >= 16, fo["radius"])
    check("森系 柔和阴影(无硬偏移)", "0px 0px" in fo["shadow"] or "rgba" in fo["shadow"], fo["shadow"])
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python3 scripts/test-themes.py
```

预期：森系三项 FAIL。

- [ ] **Step 3: 写入主题样式**

紧接着 Task 2 的段落之后插入：

```css
/* =========================================================================
   森系自然主题（data-style="forest"）—— 米绿纸面 × 大圆角 × 苔绿强调
   ========================================================================= */
[data-style="forest"] {
  --bg:        #F1F5EC;
  --paper:     #FAFCF6;
  --ink:       #232F1E;
  --ink-soft:  #4E5C46;
  --ink-faint: #889180;
  --border:    #DCE5D2;
  --accent:    #4C7A3F;
  --accent-tint:#E4EFDB;
  --danger:    #B3261E;
  --danger-tint:#F6E3E1;
  --shadow-sm: 0 1px 2px rgba(35, 47, 30, 0.05), 0 4px 12px rgba(35, 47, 30, 0.07);
  --radius:    18px;
}
[data-style="forest"][data-theme="dark"] {
  --bg:        #111A12;
  --paper:     #18211A;
  --ink:       #DCE8D4;
  --ink-soft:  #A8B8A0;
  --ink-faint: #6E7C68;
  --border:    #2A382C;
  --accent:    #8FBF7A;
  --accent-tint:#24331F;
  --danger:    #F2B8B5;
  --danger-tint:#33231F;
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3), 0 4px 14px rgba(0, 0, 0, 0.25);
}

/* 卡片左侧色条 */
[data-style="forest"] .note-item {
  border-left: 3px solid var(--accent);
  border-radius: 18px;
}
[data-style="forest"] .note-swipe { border-radius: 18px; }
[data-style="forest"] .search-wrap,
[data-style="forest"] .dialog { border-radius: 18px; }
[data-style="forest"] .sheet { border-radius: 24px 24px 0 0; }
[data-style="forest"] .btn,
[data-style="forest"] .quickbar-btn,
[data-style="forest"] .tool-btn { border-radius: 12px; }
[data-style="forest"] .segmented { border-radius: 14px; }
[data-style="forest"] .seg { border-radius: 10px; }
[data-style="forest"] .markdown-body pre,
[data-style="forest"] .markdown-body code { border-radius: 10px; }
[data-style="forest"] .markdown-body pre code { border-radius: 0; }

/* 标题左侧绿色短竖线 */
[data-style="forest"] .markdown-body h1,
[data-style="forest"] .markdown-body h2,
[data-style="forest"] .markdown-body h3 {
  border-bottom: none;
  border-left: 4px solid var(--accent);
  padding-left: 10px;
}
[data-style="forest"] .note-item-title {
  border-left: 3px solid var(--accent);
  padding-left: 8px;
}

/* 引用：柔和绿底 */
[data-style="forest"] .markdown-body blockquote {
  border-left: 3px solid var(--accent);
  background: var(--accent-tint);
  border-radius: 0 12px 12px 0;
}

/* 分割线：绿色点线 */
[data-style="forest"] .markdown-body hr {
  border: none;
  border-top: 2px dotted var(--accent);
  opacity: 0.7;
}

/* 标签：浅绿圆角 */
[data-style="forest"] .tag-chip,
[data-style="forest"] .filter-chip {
  background: var(--accent-tint);
  color: var(--accent);
  border-radius: 999px;
}

/* FAB：绿色渐变 */
[data-style="forest"] .fab {
  background: linear-gradient(150deg, var(--accent), #35602D);
  color: #FFFFFF;
  border-radius: 20px;
  box-shadow: 0 6px 18px rgba(76, 122, 63, 0.35);
}
[data-style="forest"][data-theme="dark"] .fab {
  background: linear-gradient(150deg, var(--accent), #5E8F4C);
  color: #14200F;
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python3 scripts/test-themes.py
```

预期：全部 PASS。

- [ ] **Step 5: 截图人工确认**

```bash
python3 scripts/test-themes-shot.py && open scripts/shots
```

检查 `theme-forest-*.png` 四张：左侧色条不与圆角冲突、深色下绿色可读。

- [ ] **Step 6: 提交**

```bash
git add styles.css scripts/test-themes.py
git commit -m "主题：森系自然样式（米绿纸面、大圆角、左侧色条）"
```

---

### Task 4: 版本号同步与回归

**Files:**
- Modify: `app.js:85`（若 Task 1 已改则确认）、`sw.js:7`、`version.json`

- [ ] **Step 1: 升级版本号**

确认 `app.js` 中为 `const APP_VERSION = 'v13';`（Task 1 已改）。
修改 `sw.js` 第 7 行：

```javascript
const CACHE = 'zhimo-notes-v13';
```

修改 `version.json` 为：

```json
{"version":"v13"}
```

- [ ] **Step 2: 回归全部测试**

```bash
python3 scripts/test-app.py && python3 scripts/test-features.py && python3 scripts/test-features2.py && python3 scripts/test-themes.py
```

预期：全部脚本退出码 0。

- [ ] **Step 3: 检查旧主题未被破坏**

```bash
python3 scripts/test-detect.py
```

预期：退出码 0。

- [ ] **Step 4: 提交**

```bash
git add app.js sw.js version.json
git commit -m "版本 v13：新增打字机/森系主题"
```

---

## 完成标准

- `scripts/test-themes.py` 覆盖：4 风格 × 2 明暗的属性、theme-color、持久化、两级菜单、月亮按钮行为
- 8 张主题截图人工确认通过
- `test-app.py` / `test-features.py` / `test-features2.py` / `test-detect.py` 无回归
- 版本号三处一致为 `v13`
